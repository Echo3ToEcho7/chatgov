# Congress API Key (optional but recommended for production)
# Get your free API key from: https://api.congress.gov/sign-up/
CONGRESS_API_KEY=your_congress_api_key_here

# Server Port (optional, defaults to 3001)
PORT=3001

# Server Host (optional, defaults to localhost)
# Use 0.0.0.0 to bind to all interfaces
HOST=localhost

# Client-side variables (deprecated - use server-side CONGRESS_API_KEY instead)
# VITE_CONGRESS_API_KEY=your_api_key_here